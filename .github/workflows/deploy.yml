name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 135.125.102.63
          username: debian
          key: ${{ secrets.DEPLOY_KEY }}
          script: |
            cd /srv/desarrolloyoli
            git pull origin main
